/**
 * Check if element exists
 * Hashmap
 * Collision & its resolution
 * Lamda function
 * Code implementation
 */
class `60_Internal_Implementation_And_Problems` {

    /**
     * Given an integer array and some queries,
     * for every query you need to check if the element exists or not
     */
    fun first(arr: Array<Int>) {

    }
}